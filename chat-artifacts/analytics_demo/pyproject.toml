[project]
name = "marginops-analytics-demo"
version = "0.1.0"
description = "Offline MarginOps Python demo (matplotlib + Streamlit) for the training."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "pandas>=2.0",
    "matplotlib>=3.8",
    "streamlit>=1.30",
]
